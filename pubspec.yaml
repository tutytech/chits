name: chitfunds
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.3.2
  file_picker: ^8.1.4
  image_picker: ^1.1.2
  intl: 
  path: ^1.9.0
   # Specify a version
  screenshot: 
 

  image:   # Specify a version

  permission_handler: 
  flutter_bluetooth_printer: ^2.17.1
  bluetooth_print: ^4.3.0
  flutter_blue_plus: ^1.34.5
  get: ^4.6.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
  plugin:
    platforms:
      android:
        package: com.peoplewareinnovations.bluetooth_thermal_printer
        pluginClass: BluetoothThermalPrinterPlugin
      ios:
        pluginClass: BluetoothThermalPrinterPlugin
